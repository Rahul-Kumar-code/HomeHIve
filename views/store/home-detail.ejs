<%- include('../partials/headtag') %>
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
</head>
<body class="bg-gray-50">
  <%- include('../partials/header') %>
  <main class="container mx-auto px-4 py-8">
    <div class="max-w-4xl mx-auto bg-white rounded-2xl shadow-lg p-6">
      <h1 class="text-3xl font-bold mb-6 text-gray-800">
        <%= home.homename %>
      </h1>
      <div class="space-y-6">
        <img src="<%= home.homeImg %>" class="w-full h-[400px] object-cover rounded-xl">
        <div class="flex justify-between items-start p-4 bg-gray-50 rounded-lg">
          <div class="space-y-2">
            <div class="flex items-center gap-2">
              <span class="fa fa-star"></span>
              <span class="font-medium"><%= home.rating %></span>
            </div>
            <div class="text-gray-600 text-lg">
              <%= home.location %>
            </div>
           <div class="text-xl font-bold"> About this Home</div>
            <div class="text-gray-600 text-lg  pr-2">
              <%= home.description %>
            </div>
          </div>
          <div class="text-right">
            <div class="font-bold text-xl text-gray-900">
              ₹<%= home.price %> <span class="text-gray-600 font-normal text-base">night</span>
            </div>

           <button class="bg-gradient-to-r from-purple-600 to-blue-500 px-9 py-4 mt-2 text-base rounded-lg text-white font-medium">
            Book Now
          </button>
          </div>
        </div>
      </div>
    </div>
  </main>
</body>
</html>